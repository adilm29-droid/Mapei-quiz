export default function QuizzesPlaceholder() {
  return (
    <div className="rounded-2xl border border-midnight-line bg-midnight-elevated/40 p-12 text-center backdrop-blur">
      <h1 className="text-h1 font-semibold text-white">Quizzes</h1>
      <p className="mt-3 text-caption text-whitex-muted">
        Coming next — admin will paste a 30-row CSV here and create the week's quiz.
      </p>
    </div>
  )
}
